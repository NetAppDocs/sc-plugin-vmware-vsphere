---
sidebar: sidebar
permalink: reregister-scv-vcenter.html
keywords: re-register SCV with vCenter, recover SCV registration, SCV not connected to vCenter, update vCenter credentials in SCV, unregister SCV extension
summary: 'Use this task to recover SCV registration with vCenter after credential, network, or appliance changes.'
---

= Recover SCV connection and registration with vCenter
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
SnapCenter Plug-in for VMware vSphere (SCV) registers with vCenter automatically when you deploy the appliance.
If registration is broken, for example after a vCenter password change, an SCV IP address or hostname change, a connectivity outage, or a partially failed deployment, use this task to recover the connection and registration and confirm that it is healthy.

SCV does not automatically retry a failed registration on a timer.
Registration is re-attempted only when one of the following occurs:

* The VMware vSphere client service (`scvservice`) is restarted.
* The SCV appliance IP address or hostname changes.
* The vCenter credentials are re-saved in SCV.

.Before you begin

* You must know the SCV management user interface IP address and logon credentials.
* If multi-factor authentication (MFA) is enabled, generate a fresh 6-digit MFA token from the maintenance console.
* Verify that the vCenter account used by SCV has the required privileges assigned at the vCenter root.
+
For more information, see link:scpivs44_minimum_vcenter_privileges_required.html[Minimum vCenter privileges required for the SCV].

== Recovery steps

. Restart the VMware vSphere client service.
+
A restart re-runs extension registration automatically.
For more information, see link:scpivs44_restart_the_vmware_vsphere_web_client_service.html[Restart the VMware vSphere client service for the SCV].

. Re-save the vCenter credentials.
+
If a restart does not fix the issue, log in to the SnapCenter Plug-in for VMware vSphere management user interface using the format `\https://<appliance-IP-address>:8080`.
+
Select *Configuration* > *vCenter* > *Edit*, enter the current password, and then select *Save*.

For more information, see link:scpivs44_modify_the_vcenter_logon_credentials.html[Update the vCenter credentials configured in SCV].

. Unregister and re-register the plug-in.
+
If the plug-in is still not connected (for example, the registered extension points to an old SCV IP address), unregister SCV from vCenter and then re-register by re-saving credentials or restarting the service.
+
For more information, see link:scpivs44_unregister_plugin_from_vcenter.html[Unregister the SnapCenter Plug-in for VMware vSphere from vCenter].

. Verify the registration.

Use the success checks in <<verify-registration-succeeded>>.

[[verify-registration-succeeded]]
== Verify that the registration succeeded

Use any of the following checks to confirm that SCV is registered and connected:

* *Management user interface*: On *Dashboard*, SCV shows as connected to vCenter and enabled.
* *vSphere Client*: The *SnapCenter Plug-in for VMware vSphere* menu or shortcut is present, and the plug-in pages load.
* *Logs (appliance)*: In `/var/log/netapp/vmcontrol/vmcontrol.log`, look for messages such as `Registering Aegis as a Vcenter Extension` and `Registered com.netapp.aegis extension`.
Credential changes are also recorded in `/var/log/netapp/audit/audit.log`.

[NOTE]
====
Advanced troubleshooting reference:

* SCV registers the vCenter extensions `com.netapp.aegis` (backend) and `com.netapp.scv.client` (vSphere Client remote plug-in).
* `com.netapp.scvm.webclient` is the legacy HTML client extension.
* You can view these extensions in vCenter Extension Manager.
* The REST endpoint `POST /api/4.1/vcenter` (port 8144) returns `Successfully registered extension with vCenter Server.` on success.
* `POST /api/4.1/vcenter/validate` checks connectivity and credentials.
====

== Common registration errors and remediations

* *Invalid login credentials*: Re-enter the correct vCenter username (`domain\username`) and password.
If MFA is enabled, generate a fresh 6-digit token from the maintenance console.
* *Insufficient privileges (RBAC)*: Ensure the vCenter account has the SCV-required privileges, including the privilege to register an extension, assigned at the vCenter root.
For more information, see link:scpivs44_minimum_vcenter_privileges_required.html[Minimum vCenter privileges required for the SCV].
* *Cannot connect to vCenter or connection refused*: Verify the vCenter host and port, network and firewall connectivity, and that vCenter is running.
* *Unknown host*: Verify the vCenter FQDN or IP address and verify that DNS resolution works from the appliance.
* *Unsupported vCenter version*: SCV does not support vCenter Server earlier than 6.0 Update 3.
Verify supported versions for your SCV release in link:scpivs44_deployment_planning_and_requirements.html[Deployment planning and requirements for the SCV].
* *Service not running*: If the VMware vSphere client service (`scvservice`) is stopped, registration cannot run.
Restart the service.

== Related topics

* link:scpivs44_modify_the_vcenter_logon_credentials.html[Update the vCenter credentials configured in SCV]
* link:scpivs44_restart_the_vmware_vsphere_web_client_service.html[Restart the VMware vSphere client service for the SCV]
* link:scpivs44_unregister_plugin_from_vcenter.html[Unregister the SnapCenter Plug-in for VMware vSphere from vCenter]
* link:scpivs44_minimum_vcenter_privileges_required.html[Minimum vCenter privileges required for the SCV]
+
